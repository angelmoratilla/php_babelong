{% extends 'base.html.twig' %}

{% block title %}{{ welcome_message }} - Babelong{% endblock %}

{% block body %}
<div style="margin-bottom: 1em;">
    <div class="example-wrapper">
        <h1>{{ welcome_message }} - Panel de Administración</h1>

        <div class="user-info">
            <p><strong>Usuario:</strong> {{ app.user.username }}</p>
            <p><strong>Email:</strong> {{ app.user.email }}</p>
            <a href="{{ path('app_logout') }}" class="btn btn-danger btn-sm">Cerrar Sesión</a>
        </div>

        <p>
            Bienvenido al panel de administración de <strong>Babelong</strong>.
        </p>

        <div class="card card-success">
            <h2>🎉 ¡Proyecto configurado exitosamente!</h2>
            <ul>
                <li>✅ Symfony {{ constant('Symfony\\Component\\HttpKernel\\Kernel::VERSION') }}</li>
                <li>✅ Twig para plantillas</li>
                <li>✅ Sistema de formularios</li>
                <li>✅ Validador de datos</li>
                <li>✅ Gestión de assets</li>
                <li>✅ <strong>Base de datos MySQL conectada</strong></li>
                <li>✅ <strong>Doctrine ORM configurado</strong></li>
            </ul>
        </div>

        <div class="card card-info">
            <h3>🗄️ Base de Datos</h3>
            <p>Tu aplicación ahora está conectada a la base de datos MySQL <strong>"babelong"</strong> con acceso completo a todas las tablas existentes.</p>
            <p>
                <a href="{{ path('app_database_stats') }}" class="btn btn-primary">
                    📊 Ver Estadísticas de la BD
                </a>
                <a href="{{ path('app_users') }}" class="btn btn-success">
                    👥 Ver Usuarios
                </a>
            </p>
        </div>

        <div class="card card-info">
            <p>
                <strong>Controlador:</strong> <code>{{ controller_name }}</code><br>
                <strong>Plantilla:</strong> <code>templates/home/index.html.twig</code>
            </p>
        </div>

        <p>
            <a href="{{ path('app_about') }}" class="btn btn-primary">
                Ver información del proyecto
            </a>
            <a href="{{ path('app_admins') }}" class="btn btn-info">
                👤 Gestionar Administradores
            </a>
            <a href="{{ path('app_landing') }}" class="btn btn-secondary">
                🏠 Volver al Inicio Público
            </a>
        </p>
    </div>
</div>
{% endblock %}
